# Changelog

## [Unreleased]

- Enforce vault-wide unique note names: block creating, moving, or renaming a note into a name that already exists elsewhere in the vault.
- Only act while the vault's new link format is "Shortest path when possible" (configurable).
- Leave pre-existing duplicates untouched — only new collisions are blocked.
- Add an optional copy guard (off by default).
- Add a settings tab with a toggle for each guard.
- Require Obsidian 1.8.7 or newer.

## [0.0.200]

- bump
